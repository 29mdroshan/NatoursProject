import { TourFilterPipeDirective } from './tour-filter-pipe.directive';

describe('TourFilterPipeDirective', () => {
  it('should create an instance', () => {
    const directive = new TourFilterPipeDirective();
    expect(directive).toBeTruthy();
  });
});
